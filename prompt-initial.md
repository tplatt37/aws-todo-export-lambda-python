Using Python and SAM - I need to generate a VERY SIMPLE Lambda function that will operate as follows: Triggered by an SQS Queue message , it should SCAN a DynamoDB table named "TodoITems-Dev" (Make this configurable via ENvironment Variable) and produce a CSV output (with header row) of all the Items (including all attributes).  The CSV OUtput should be stored as a .csv file in an S3 bucket (Bucket Name configured in Env variables).   The Lambda should then generate a PRE-SIGNED URL that expires in 5 minutes to allow a user to download the file.  Lastly, it should drop a message into an SNS Topic (configurable via Env var) that includes the Pre-signed URL.  The user will be responsible for having a email subscription to this topic.  Please note the Queue, Bucket, Topic, and DynamoDB table all exist already.    Secondly keep the Python code AS SimplE AS POSSIBLE.  This function will be used to teach students about AWS lambdas - don't use any "requirements.txt" file. Ideally you will produce just a single, short .py file.  Don't go crazy with error catching and checking simply print any issues or problems.
